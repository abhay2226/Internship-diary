Work Summary :
Continued with JavaScript Session : Control flow, Conditional statements , Loops, and types such as for loop, while loop , break and continue statements and Functions 
with some of practical examples like check if number is even or odd 

function startGame() {
    let randomNumber = Math.floor(Math.random() * 100) + 1;
    let attempts = 0;
    let guess;

    while (guess !== randomNumber) {
        guess = parseInt(prompt("Guess a number between 1 and 100:"));
        attempts++;

        if (guess > randomNumber) {
            console.log("Too high! Try again.");
        } else if (guess < randomNumber) {
            console.log("Too low! Try again.");
        } else if (guess === randomNumber) {
            console.log("Correct! You guessed it in " + attempts + " attempts.");
        } else {
            console.log("Invalid input");
        }
    }
}

startGame();

Learning Outcomes :

Understood the concepts of javascript , learnt to write the simple programs in javascript
